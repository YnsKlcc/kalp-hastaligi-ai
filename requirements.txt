# ============================================================
# Klinik AI Asistan - Python bağımlılıkları
#
# Kurulum:  pip install -r requirements.txt
# Geliştirme ve test ortamı: Python 3.13
#
# Sürümler `pip freeze` çıktısından alınmıştır; yani burada yazan
# sürümler projenin GERÇEKTEN test edildiği sürümlerdir. Elle
# güncellerken de aynısını yapın: önce kurun, test edin, sonra
# freeze çıktısını buraya yansıtın.
# ============================================================

# --- Web çatısı ve sunucu ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4

# --- Hız sınırlama / kaba kuvvet koruması ---
slowapi==0.1.10
# limits, slowapi'nin bağımlılığıdır AMA main.py onu doğrudan import
# eder (MovingWindowRateLimiter ile başarısız giriş sayacı). Dolaylı
# bağımlılığa doğrudan import yapılmaz: slowapi bir gün limits'i
# bırakır veya sürümünü değiştirirse kaba kuvvet koruması haber
# vermeden kırılırdı. Bu yüzden açıkça sabitlenir.
limits==5.8.0

# --- Ortam değişkenleri (.env okuması) ---
python-dotenv==1.2.3

# --- PDF rapor üretimi (/pdf-indir) ---
reportlab==5.0.1

# --- Veri işleme ---
pandas==3.0.5
numpy==2.5.2

# ------------------------------------------------------------
# DİKKAT: aşağıdaki iki sürüm model.pkl ile bağlıdır.
# model.pkl bu sürümlerle üretilmiştir; farklı bir scikit-learn
# sürümünde model ya hiç yüklenmez ya da sessizce hatalı sonuç
# üretir. Yükseltilecekse `python model_egit.py` yeniden
# çalıştırılıp model.pkl yeniden üretilmelidir.
# ------------------------------------------------------------
scikit-learn==1.9.0
joblib==1.5.3

# Not: sqlite3, hmac, hashlib, secrets, logging Python standart
# kütüphanesinin parçasıdır; ayrıca kurulmaları gerekmez.
# Testler (tests/) yalnızca standart kütüphaneyi kullanır;
# pytest isteğe bağlıdır: `python tests/test_klinik.py` yeterlidir.
